// This file previously defined CourseContent-related types.
// Per project conventions we now keep canonical types in `src/types/course.ts`.
// Re-export them here for compatibility with any existing imports.

export * from "./course";

// NOTE: prefer importing directly from `src/types/course` going forward.
